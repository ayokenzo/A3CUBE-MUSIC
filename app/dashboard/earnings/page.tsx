import { Workspace } from "@/components/DashboardNav"; import { Card } from "@/components/Shell"; import { earnings } from "@/lib/data"
export default function Earnings(){return <Workspace><h1 className="text-4xl font-black">Earnings</h1><div className="mt-8 grid gap-5 md:grid-cols-3">{earnings.map(e=><Card key={e.source}><p className="text-slate-400">{e.source}</p><p className="mt-3 text-3xl font-black">{e.amount}</p><p className="mt-2 text-emerald-300">{e.change} this month</p></Card>)}</div><Card className="mt-8"><h2 className="text-2xl font-black">Payout readiness</h2><p className="mt-3 text-slate-300">Connect bank details, review splits, and request withdrawals once minimum payout is reached.</p></Card></Workspace>}
